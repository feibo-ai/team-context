# multica 字段默认值 — 已迁移

**正文单源已迁至 [`skills/tc-render/references/multica-fields.md`](../skills/tc-render/references/multica-fields.md)**
（issue/project 字段默认值 + `$ME_NAME`/`$ME_UID` 运行时解析规则，随 skill 分发到每台机器）。

- 改字段默认值：改 tc-render 的 references 文件 + PR review
- 本存根保留 90 天（至 2026-10-01 前后删除），期间旧链接不断
